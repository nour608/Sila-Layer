# CRE workflow settings — settlement-workflow (Slice 3)
# Cron-poll a pending-checkouts endpoint and submit DON-signed reports to SettlementReceiver.

staging-settings:
  user-workflow:
    workflow-name: "Sila LayerSettlement-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "./secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "Sila LayerSettlement-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "./secrets.yaml"
