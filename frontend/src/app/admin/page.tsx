import AdminPanel from "@/components/admin/AdminPanel";

export const metadata = {
  title: "Sila Layer — Admin",
  description: "Demo admin control panel",
};

export default function AdminRoute() {
  return <AdminPanel />;
}
