import Dashboard from "@/components/dashboard/Dashboard";

export const metadata = {
  title: "Sila Layer — Dashboard",
  description: "Merchant settlement dashboard",
};

export default function DashboardRoute() {
  return <Dashboard />;
}
